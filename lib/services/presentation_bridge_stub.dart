Future<void> generateProductPresentation(
    List<Map<String, Object?>> products,
    String fileName,
    ) async {
  throw UnsupportedError(
    'PowerPoint generation is currently available on Web only.',
  );
}